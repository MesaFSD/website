# MesaFSD

Website for San Diego Mesa College's Full-Self Driving Club — [mesafsd.com](https://mesafsd.com).

## Getting Started

```bash
npm install
npm run dev
```

Open [http://localhost:3000](http://localhost:3000).

## Scripts

- `npm run dev` — development server
- `npm run build` — production build
- `npm run start` — run production server
- `npm run lint` — ESLint
- `npm run favicon:rounded` — regenerate rounded favicon from `public/logo/mesafsd.png`
